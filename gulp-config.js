'use strict'

const paths = {
  src : 'src',  // source parent folder
  out : 'dist',  // output parent folder
  assets : 'assets',
  templates : 'templates',  // input of nunjucks templates
}

module.exports = {
  paths
}
